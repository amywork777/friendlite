# Railway deployment environment variables
# Copy these to your Railway project environment variables

# Authentication
AUTH_SECRET_KEY=your-super-secret-jwt-key-change-this-to-something-random-and-long-123456
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@example.com

# LLM Configuration - OpenAI API Key
LLM_PROVIDER=openai
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Speech-to-Text - Deepgram API Key
DEEPGRAM_API_KEY=your-deepgram-api-key-here
TRANSCRIPTION_PROVIDER=deepgram

# Database Configuration (Railway will provide these URLs)
MONGODB_URI=${MONGO_URL}
QDRANT_BASE_URL=${QDRANT_URL}

# Memory Provider
MEMORY_PROVIDER=friend_lite

# Network Configuration (Railway will set PORT automatically)
HOST=0.0.0.0
PORT=${PORT}
BACKEND_PUBLIC_PORT=${PORT}
WEBUI_PORT=5173

# CORS Origins (update with your Railway app URL)
CORS_ORIGINS=https://your-app-name.railway.app,http://localhost:5173,http://localhost:3000

# Disable telemetry
LANGFUSE_ENABLE_TELEMETRY=False